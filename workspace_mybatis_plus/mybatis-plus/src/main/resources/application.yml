spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ssm?serverTimezone=GMT%2B8&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: qiusheng..

mybatis-plus:
  #    mybatis-plus的日志功能
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
#  #设置mybatis-plus的全局配置
#  global-config:
#    db-config:
#      #设置实体类所对应的表的统一前缀
#      table-prefix: t_
#      #设置统一的主键生成策略
#      id-type: AUTO
#  #配置类型别名所对应的包
#  type-aliases-package: com/atguigu/mybatisplus/entity
